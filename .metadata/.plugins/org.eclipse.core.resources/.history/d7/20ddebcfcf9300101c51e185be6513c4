package naissance;
/*** Ma classe Main 
 * Elle utilise un constructeur pour créer un objet "MonChien"
 * 
 *J'appelle un constructeur de la classe Monchien avec 5 parametres (le constructeur n'est pas inclus ici)
 *car il se trouve dans la Classe Monchien et pas dans le main!!!
 * 
 * 
 * 
 * ***/

//ICI mon programme demare
public class Main{ //debut classe main 
	public static void main(String[] args) {
		//Il demarre ici
		Monchien bill = new Monchien("bill",5, Sexe.MALE, Race.LABRADOR, Color.BLUE); //creation de l'objet Monchien qui s'appelle bill
	    bill.aboyer(); // on appelle la méthode aboyer() de mon objet bill puis affiche wouf
	    bill.carteidentite();
	    
	}
} //fin classe main

/***
 * Dans la classe Main que j'ai crée ici, elle va cree un objet "Monchien"
 * Le constructeur que j'utilise est defini dans la classe Monchien (donc pas ici)
 * 
 * Avec LES VALEURS que j'ai donnée a bill , bill devient une instance concrete de la
 * classe Monchien 
 */